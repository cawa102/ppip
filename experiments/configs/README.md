# Experiment Configs

This directory stores schemas and configuration files for benchmark conditions.

The fixed evaluator should validate every candidate against `attack_candidate.schema.json` before rendering or rollout evaluation.
