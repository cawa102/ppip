# Candidate Files

Store visual prompt candidates as JSON files that conform to `experiments/configs/attack_candidate.schema.json`.

Use separate files per candidate. Do not overwrite candidate files after they have been evaluated.
