# Experiment Results

Store aggregate summaries here. Heavy raw outputs should go under `runs/` and remain untracked if this workspace becomes a git repository.

Expected summary artifacts:

- `pilot-001-summary.md`
- `condition_comparison.csv`
- `best_candidates.jsonl`
- `failure_modes.md`
