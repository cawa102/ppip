# Data

Use this directory for local data references and processed artifacts.

- `external/` — downloaded or cloned datasets/checkpoints metadata.
- `processed/` — derived lightweight data tables.

Do not commit model checkpoints or large rollout dumps.
