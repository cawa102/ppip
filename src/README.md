# Source Layout

- `evaluator/` — fixed benchmark evaluator. Search agents must not modify it during controlled runs.
- `autoresearch_loop/` — editable autonomous search loop and candidate proposal logic.
- `rendering/` — LIBERO scene modification and visual prompt rendering utilities.
